{
  "name": "function-frontline-salesforce",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "jsforce": "^1.10.1",
    "jsonwebtoken": "^8.5.1",
    "twilio": "^3.67"
  },
  "devDependencies": {
    "twilio-run": "^3.2.0"
  },
  "engines": {
    "node": "12"
  }
}
